# Carregando os pacotes
library(tidyverse)
library(beepr)

# Cumprimentando o R
print('Olá!')

# Vamos fazer o R cantar?
library(beepr)
beep(sound=8)

# Criando uma figura com R: abra o script 1 - olympic_flag.R

# Armazenando dados
## Vetor com valores 1, 2 e 3
dados <- c(1,2,3)

## vetor com valores sequenciais de 1 à 10
dados <- seq(1:10)

## matriz de dados com valores de 1 a 9 distribuídos sequencialmente em 3 linhas e 3 colunas
dados <- matrix(1:9, nrow = 3, ncol = 3)


